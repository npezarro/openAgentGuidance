# Exempt lines matching these regexes. Keep entries NARROW: exempt the specific
# example, never the pattern that catches it. Anything added here is a hole.
#
# The scanner's own selftest fixture (AWS's published example key).
AKIAIOSFODNN7EXAMPLE
# Documentation examples using obvious placeholder credentials on localhost.
postgresql://test:test@localhost
